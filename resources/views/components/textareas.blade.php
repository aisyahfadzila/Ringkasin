<div class="flex flex-wrap">
    <textarea name={{ $name }} cols="30" rows="10" placeholder={{ $placeholder }} class="rounded-lg overflow-auto resize-y w-[920px] relative z-10 {{ $addon }} text-[20px] py-[10px] px-[15px]"></textarea>
    {{ $slot }}
</div>
